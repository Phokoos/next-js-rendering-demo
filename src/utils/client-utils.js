import 'client-only';

export const clientSideFunction = () => {
    console.log(
        `handle user interactions,
       manage component state,
       access browser APIs,
       render UI elements`
    );
    return "client result";
}